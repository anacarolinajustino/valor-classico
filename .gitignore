# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.eggs/

# Ambiente virtual
.venv/
venv/
env/

# Banco de dados (gerado em runtime)
instance/
*.db
*.db-shm
*.db-wal

# Logs
*.log
/tmp/

# Backups do banco local (gerados pela tarefa agendada, não versionar)
backups/

# macOS
.DS_Store

# VS Code
.vscode/

# Variáveis de ambiente locais (nunca commitar)
.env

# Curadoria do catálogo definitivo (/admin/catalogo). Muda a cada clique e é
# trabalho da usuária, não código — a pasta está no OneDrive, que é o backup.
# O módulo trata a ausência do arquivo como "nada triado ainda".
data/catalogo_definitivo.csv
